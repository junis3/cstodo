## Preprocess cstodo and cs problem solving history


Preprocess cstodo.txt and history.txt which is ignored.



`yarn`

`yarn preprocess`

## Run

Write

`export const signingSecret = 'YOUR SLACKBOT SIGNING SECRET';`
`export const accessToken = 'YOUR SLACKBOT ACCESS TOKEN';`

in `src/config.ts` (after making the file)

Execute

`yarn`

`yarn start`